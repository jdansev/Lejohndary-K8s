apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-volumes-misc-intree-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: docker-volumes-misc-intree-nfs-pv
  storageClassName: ""

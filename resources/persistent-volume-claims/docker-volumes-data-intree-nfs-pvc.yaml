apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-volumes-data-intree-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: docker-volumes-data-intree-nfs-pv
  storageClassName: ""

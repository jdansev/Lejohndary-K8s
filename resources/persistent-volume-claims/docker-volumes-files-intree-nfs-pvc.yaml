apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-volumes-files-intree-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: docker-volumes-files-intree-nfs-pv
  storageClassName: ""

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-volumes-apps-intree-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: docker-volumes-apps-intree-nfs-pv
  storageClassName: ""

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meilisearch.fullname" . }}-test-connection"
  labels:
    {{- include "meilisearch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meilisearch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

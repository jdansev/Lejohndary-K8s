apiVersion: v1
kind: Service
metadata:
  name: {{ include "atmoify.fullname" . }}
  labels:
    {{- include "atmoify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atmoify.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yt-dlp-web-ui.fullname" . }}-test-connection"
  labels:
    {{- include "yt-dlp-web-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yt-dlp-web-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

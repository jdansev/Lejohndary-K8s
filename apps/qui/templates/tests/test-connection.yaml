apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qui.fullname" . }}-test-connection"
  labels:
    {{- include "qui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

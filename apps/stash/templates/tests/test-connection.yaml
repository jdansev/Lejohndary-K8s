apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stash.fullname" . }}-test-connection"
  labels:
    {{- include "stash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stash.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

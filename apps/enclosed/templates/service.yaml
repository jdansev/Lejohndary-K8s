apiVersion: v1
kind: Service
metadata:
  name: {{ include "enclosed.fullname" . }}
  labels:
    {{- include "enclosed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enclosed.selectorLabels" . | nindent 4 }}
